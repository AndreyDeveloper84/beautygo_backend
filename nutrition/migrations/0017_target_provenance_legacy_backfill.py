"""Существующим ориентирам — честное имя происхождения, а не удобное.

DRF-1623, срез N-d. Дополняет схемную ``0016_target_provenance``.

## Что делает

Ставит ``targets_source = "unknown_legacy"`` строкам, у которых ориентир
УЖЕ ЕСТЬ (``daily_kcal > 0``). Больше ничего: ни одно существующее
значение не меняется, ориентиры не гасятся, входы не трогаются.

## Почему не ``ayla_calculated``

Соблазн понятен: эти ориентиры посчитала Ayla, значит и подписать их так.
Но это утверждение, которого мы не можем проверить.

Замер пилота 10.09.2026 (`docs/PILOT_MEASUREMENTS.md`): шесть профилей.
У **двух из шести** полный набор ориентиров при пустых весе, росте и
возрасте — то есть числа выведены из подставленной пензенской медианы
(``water = 2100 = 30 × 70``), а не из данных человека. Для этих двух
ответ известен, и он не ``ayla_calculated`` в том смысле, который §92
вкладывает в это слово: считала Ayla, но НЕ ПРО ЭТОГО ЧЕЛОВЕКА.

Для остальных четырёх ответ неизвестен, и различить их постфактум нечем:
поля снимка входов тогда не существовало. Записать всем шести
``ayla_calculated`` значило бы утверждать про четверых то, что проверено
на двоих, — и наоборот.

## Почему отдельное имя, а не пустое поле

Пустое поле неотличимо от «миграция не доехала», и первый же читатель,
которому мешает пустота, заполнит её умолчанием. Названное состояние
пустоту не создаёт: у ``unknown_legacy`` есть работа — выяснить или
пересчитать, — и очередь на эту работу существует, пока имя стоит.

## Чего эта миграция НЕ решает

Судьбу шести профилей. Станут ли их ориентиры отсутствующими — срез N-b
и открытый вопрос владельцу OD-NUT-5. Здесь строки только получают имя
своего происхождения; что с ними делать дальше, решает владелец, а не
миграция.

Обратная операция возвращает поле к умолчанию ``none`` — то есть к
состоянию «происхождения не хранили», из которого миграция и пришла.
"""

from django.db import migrations


def name_legacy_provenance(apps, schema_editor):
    NutritionProfile = apps.get_model("nutrition", "NutritionProfile")
    # Ориентир ЕСТЬ — значит у него есть и происхождение, просто мы его
    # не сохраняли. Строки без ориентира остаются с умолчанием `none`:
    # у них происхождения нет, потому что нечему принадлежать.
    NutritionProfile.objects.filter(daily_kcal__gt=0).update(targets_source="unknown_legacy")


def unname(apps, schema_editor):
    NutritionProfile = apps.get_model("nutrition", "NutritionProfile")
    NutritionProfile.objects.filter(targets_source="unknown_legacy").update(targets_source="none")


class Migration(migrations.Migration):
    dependencies = [("nutrition", "0016_target_provenance")]

    operations = [migrations.RunPython(name_legacy_provenance, unname)]
