"""Recommendation Resolver — единственный авторитет `RecommendationDecision`.

Контракт: `docs/specs/RECOMMENDATION_RESOLVER_CONTRACT_v1.0.md` (нормативный).
Решение владельца о месте исполнения: `docs/OPEN_DECISIONS.md` §53 —
модуль внутри бэкенда Ayla, **за границей контракта**, вынос в
`ayla-ai-core` после пилота.

Публичная поверхность приложения — **ровно один модуль**: `recommendation.api`.
Всё остальное приватно (`_types`, `_stages`, `_pipeline`, `_evidence`,
`_rotation`, `_reason_codes`) и не импортируется извне: граница проверяется
тестом, а не договорённостью.

Почему так, а не «пакет с удобными импортами»: до этого модуля решение
о порядке принимали три независимые реализации, две из них — в одном
процессе (аудит `RECOMMENDATION_PATHS_AUDIT.md` §5.2). Разошлись они не
потому, что кто-то спорил, а потому, что у формулы не было владельца
и любой мог написать свою рядом. Приватность здесь — не стиль, а способ
сделать четвёртую формулу невозможной без красного билда.
"""
