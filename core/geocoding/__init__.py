"""Адаптер геокодера: адрес места → координаты с происхождением (§139, DRF-1662).

Что здесь и чего здесь нет
--------------------------

Здесь **контракт исхода** и **путь записи** в ``Tenant``: как ответ любого
провайдера превращается в один из шести статусов ``GeocodeStatus`` и в восемь
полей ``#333``. Провайдер — деталь, которая меняется; отображение исходов —
то, что не должно меняться вместе с ним.

Здесь **нет** реализации DaData и Nominatim. Они придут отдельно, после двух
входов, которые адаптер закрыть не может:

* **Р1** — право DaData хранить координаты у себя. Оферта прочитана
  (``RESEARCH_MAPS_DISTANCE_2026-09-11.md`` §8): пунктов о хранении нет
  вообще, «не запрещено явно». Письмо в поддержку всё равно стоит отправить —
  п. 2.3 позволяет менять оферту публикацией. Названный предел, не блокер.
* **Р4** — §139 называет Яндекс прямо. Владелец сказал «карты очень дорого,
  поищи бесплатные»; формально §139 надо амендить, и это внесено в реестр
  решений. Яндекс здесь — заглушка с суммой в тексте, чтобы следующий не
  «дописал» его молча.

В каталоге уже есть второй геокодер — и он не этот
--------------------------------------------------

``services/geocoding.py`` (с 25.04.2026): **обратное** геокодирование
(координаты → город) через Яндекс, зовётся из ``services/pricing.py``.
Заменой не является — здесь прямое, — но два модуля геокодирования с разными
провайдерами и без связи через месяц никто не распутает. Поэтому:

* набор модулей, которые ходят к внешним геокодерам, **закрыт и объявлен**
  (``core/geocoding/providers/__init__.py``), и сторож проверяет, что третьего
  не появилось;
* тот модуль мёртв (``YANDEX_GEOCODER_API_KEY`` есть только в тестах) при
  нулевой экспозиции (у клиентов нет координат) — отдельная задача, класс
  DRF-1656. Здесь он не трогается.

Ловушка лицензии, которая меняет форму адаптера
-----------------------------------------------

Оферта DaData, п. 4.2.1: «запрещается использовать сервис „Подсказки" для
автоматической обработки адресов». Легальный паттерн из их же документации:
**человек ввёл адрес → один запрос ``count=1`` → координаты и ``qc_geo``**.

То есть адаптер строится под §139 «при сохранении или изменении адреса»,
а не под «прогнать таблицу пачкой». Команда ``geocode_tenants`` для разового
заполнения одиннадцати адресов, введённых людьми, под это подпадает; пачка
в тысячу адресов из импорта — нет, и команда об этом печатает.
"""
