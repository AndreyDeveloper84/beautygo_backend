"""Снятие ``ClientGoal.tenant`` и его индекса (DRF-1472).

Почему это новая миграция, а не удаление ``0003``/``0004``
----------------------------------------------------------

``0003``/``0004`` уже применены на боевом пилоте: слияние PR #286
(``ddda863``) в ``dev`` выложило их вместе с кодом. Удалить файлы
значило бы оставить на пилоте колонку, которую больше никто не снимет,
и строки в ``django_migrations``, ссылающиеся в пустоту. Поэтому
эффект отменяется вперёд — миграцией, — а история остаётся историей.

Обратимость — не формальность
------------------------------

``RemoveIndex``/``RemoveField`` обратимы сами: Django знает снятое
состояние и умеет вернуть колонку и индекс. Вернутся они пустыми — и
это точный откат, а не потеря: ``0004`` проставлял салон выводом из
связей клиента, то есть данными, которые никуда не делись и из которых
он проставится заново, если ``0004`` когда-нибудь снова понадобится.
Ничего, что существовало бы только в этой колонке, в ней не лежало.

Данных, которые надо перенести, нет: целей на пилоте две.
"""
from __future__ import annotations

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("goals", "0004_backfill_clientgoal_tenant"),
    ]

    operations = [
        migrations.RemoveIndex(
            model_name="clientgoal",
            name="clientgoal_tenant_active_idx",
        ),
        migrations.RemoveField(
            model_name="clientgoal",
            name="tenant",
        ),
    ]
