Follow-up задачи для окна W1. РАЗБЛОКИРОВАНО 2026-07-19: код W2 (billing/)
смержен в dev (41a5133c). Применяй патчи в своей зоне (ты владелец файлов,
PILOT_CONTRACTS §8).

ОЧЕРЕДЬ ПАТЧЕЙ:

P1. settings/base.py: INSTALLED_APPS += 'billing', (после этого W2 удаляет свой
    временный --ds-шим billing/tests/settings_w2.py и billing/tests/urls_w2.py).
P2. CELERY_BEAT_SCHEDULE (точные записи от W2):
    - billing.tasks.charge_subscriptions_monthly — crontab(hour=4, minute=30)
    - billing.tasks.retry_failed_subscription_charges — crontab(hour=5, minute=15)
P3. djangoProject/urls.py (точные пути от W2):
    path("api/v1/internal/billing/", include("billing.internal_urls")),  # C2 status + card-setup (Bearer)
    path("api/v1/internal/billing/", include("billing.urls")),           # webhook/ (YooKassa: IP allowlist + Basic)
P4. Реестр продьюсера событий (W2 R-2): appointments/models.py OutboxEvent.Topic +
    appointments/infrastructure/outbox/envelope.py EVENT_VERSIONS += 3 топика
    (subscription.activated, subscription.past_due, billing.fee_charged — все v1)
    + миграция на appointments. Envelope — полный формат event-contract (data/actor/
    causation_id), event_id — UUID4 строка (AMD-008).
P5. Хук fee-начисления (W2 R-5): billing.handlers.on_booking_completed на
    booking.completed. ВАЖНО (уточнение W2): EVENT_HANDLERS.update(BOOKING_HANDLERS)
    перезаписывает запись — нужен CHAIN с notifications.handle_booking_completed,
    НЕ замена; billing-хендлер ставим ПЕРВЫМ (деньги раньше пушей); его хендлер
    исключения наружу не бросает.
P6. Совместный инвариант-тест W1×W2 (C1): твой адаптер billing_eligibility +
    реальный billing/services.py::can_accept_booking — проверить: past_due → 409
    SUBSCRIPTION_PAST_DUE (internal) / generic UNAVAILABLE (клиентский), salon-подписка
    блокирует всех мастеров tenant, fail-open при ошибке billing, блокируются только
    НОВЫЕ записи (cancel/complete/reschedule — нет). Плюс инвариант одиночного
    взыскания fee (AMD-009/AMD-011): authorized/paid/refunded/partially_refunded →
    fee НЕ начисляется; failed/pending → начисляется; ровно один раз.
P7. Проверка слотов (вопрос W3, O1): все datetime в слотах internal API несут
    timezone offset (ISO 8601 с tz по §1); если где-то naive — в отчёт, не чинить
    молча.
P8. ⚠️ ВАЖНО (находка W2): payments webhook /api/v1/payments/webhook/ НЕ входит в
    EXCLUDED_PATH_PREFIXES (users/middleware.py:66) — AppTypeMiddleware отвечает 403
    вызовам без X-App-Type, а реальный YooKassa заголовок не шлёт. Вероятно, prod
    webhook клиентских платежей сейчас 403-ится. ПРОВЕРИТЬ и исправить (добавить в
    EXCLUDED_PATH_PREFIXES) — приоритет выше P1, тесты payments шлют заголовок сами,
    поэтому в тестах не видно. Сравни с решением W2 для billing webhook (AMD-014).

Правила прежние: коммиты conventional, тесты зелёные перед отчётом, merge в dev
не сам — передаёшь оркестратору.
