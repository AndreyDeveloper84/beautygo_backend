# Внешние вопросы пилота (External Questions Register)

Создан: 2026-07-19. Владелец реестра: оркестратор. Статусы обновляются по факту.
Правила: каждый вопрос имеет владельца, дедлайн и список блокируемых задач.
Вопрос закрывается только фактом (доступ/документ/подтверждение), не словами.

| # | Вопрос | Владелец | Дедлайн | Статус |
|---|---|---|---|---|
| E1 | Staging-инфраструктура | Founder | 2026-07-21 | 🔄 частично решено |
| E2 | Тестовый магазин ЮKassa | Founder | 2026-07-22 | ⏳ ожидает |
| E3 | MAX staging-бот | Founder | 2026-07-22 | ⏳ ожидает |
| E4 | Список пилотных мастеров (15+) | Founder | 2026-07-25 | ⏳ ожидает |

---

## E1 — Staging-инфраструктура

**Вопрос:** `dev.gobeauty.site` — это действующий staging или нужен новый стенд?
Где поднимать bot staging (ai-bot-platform) — тот же VPS?

**Решено 2026-07-20:**
- `dev.gobeauty.site` подтверждён фактами как staging Ayla backend (nginx+TLS,
  `/home/taximeter/beautygo/dev`, deploy.sh compose-based).
- **Bot staging — на том же VPS**, отдельный compose-проект `ayla-bot-staging`
  (вариант A из SPRINT_2_STAGING_SETUP v2): bot web 127.0.0.1:8001, смещённые
  порты, отдельные volumes/networks/env, compose под одним systemd-юнитом.
- Домены: `bot-staging.gobeauty.site` (bot), `app-staging.gobeauty.site` (miniapp).

**Осталось для закрытия (3 факта):**
1. Размер VPS (CPU/RAM — нужно ≥4 CPU/8 GB; если меньше — поднимаем только
   нужные компоненты, без Chroma/MinIO).
2. SSH-доступ (кто имеет, sudo, ключи) — для деплоя и миграций.
3. DNS-контроль gobeauty.site (где управляется) — для поддоменов bot-/app-staging.

**Факты 2026-07-20 (проверено напрямую):**
- VPS: Rucloud Королёв, Ubuntu 18.04, **2 CPU**, 8 GB RAM (доступно ~4 GB), 80 GB HDD.
  IP `194.87.99.126`, Docker 28.1.1. CPU=2 — подтверждённый лимит: staging-профиль
  урезанный (без ChromaDB на старте, gunicorn/celery с ограниченной concurrency).
- SSH: доступ есть (ключи на месте, taximeter@194.87.99.126).
- DNS: панель reg.ru (аккаунт tikhonov-a-s@yandex.ru), 3 домена, `gobeauty.site`
  до 28.02.2027. **Новые записи не нужны** — существующие поддомены покрывают всё:
  `dev` → Ayla backend (жив), `api-dev` → bot backend (502, поднять),
  `miniapp-dev` → Mini App (старый билд есть, обновить до dev).
- ~~⚠️ Диск заполнен на 95%~~ → **РЕШЕНО 2026-07-20:** docker builder prune
  освободил ~39 GB (build cache); диск 43% занято, 43 GB свободно.

**Блокирует:** весь спринт 2 (S0–S6): прогон link coverage, round-trip событий,
деньги на staging, smoke-runner, rehearsal.
**Fallback:** один VPS с docker-compose обоих backend (минимум), staging-флаг.

## E2 — Тестовый магазин ЮKassa

**Вопрос:** есть ли тестовый магазин ЮKassa (shopId + secret key для тестовых
платежей)? Если нет — регистрируем сейчас (это минуты в кабинете ЮKassa).

**Что нужно:**
1. shopId + secret key тестового магазина (в env staging, не в код).
2. Доступ в кабинет ЮKassa — для настройки webhook URLs (2 шт.):
   - payments: `https://<ayla-staging>/api/v1/payments/webhook/`
   - billing: `https://<ayla-staging>/api/v1/internal/billing/webhook/` (AMD-014)
3. Тестовая карта (номер из документации ЮKassa — для smoke).

**Блокирует:** S3 (деньги на staging: hold→capture→split, подписка, dunning).

## E3 — MAX staging-бот

**Вопрос:** есть ли тестовый MAX-бот (токен) для staging, или создаём нового?
Также: staging Mini App URL — регистрировать отдельно?

**Что нужно:**
1. `MAX_BOT_TOKEN` staging-бота (в env bot staging).
2. Решение: тот же бот с test-окружением или отдельный staging-бот.
3. `MAX_MINIAPP_URL` для staging (webview URL miniapp на staging).

**Блокирует:** E2E-сценарии через канал (запись/напоминания/memory-ask в DM),
smoke 5–7.

## E4 — Список пилотных мастеров (15+)

**Вопрос:** кто конкретно из мастеров «Формулы тела» (или вне его) идёт в пилот
Пензы? Нужен именованный список ≥15.

**Что нужно:**
1. Список: имя, специализация (услуги), контакт, статус согласия.
2. Для каждого — данные для KYC в ЮKassa (паспорт/ИНН/реквизиты по форме
   самозанятого или ИП) — процесс на 3–5 дней на мастера, старт немедленно.
3. Назначить ответственного за онбординг (кто звонит/сопровождает).

**Блокирует:** supply пилота (без мастеров пилот пустой), link coverage по
реальным услугам, S5.1 спринта 2, метрики пилота (≥50 мастеров к M4).

---

## Связанные внешние задачи (трек отдельно, PILOT_PROGRESS)

- Оферта автоплатежа (юрист) — дедлайн 01.08
- Агентская формулировка чеков split (юрист) — дедлайн 01.08
- Правки 3/5 cross-domain правил (юрист) — дедлайн 01.08
- KYC-онбординг мастеров — операционно, после E4

## Журнал

- 2026-07-19: реестр создан (E1–E4 зафиксированы по итогам спринта 1).
